# =====================================================================
# MOTIS LEADFLOW - LOCAL ENVIRONMENT CONFIGURATION
# =====================================================================
--
# INSTRUCTIONS:
# 1. Copy this file and rename the copy to exactly: `.env`
# 2. Paste your private Supabase keys below (obtained from your Supabase dashboard settings -> API).
# 3. WARNING: Never commit your actual `.env` file to Git! 
#    It is already ignored by default to protect your credentials.
#
# =====================================================================

# Your Supabase Project URL (looks like https://xxxxxxxxxxxx.supabase.co)
SUPABASE_URL=your_supabase_project_url_here

# Your Supabase Secret  Key (BYPASSES Row Level Security - DO NOT expose publicly!)
# Located under Project Settings -> API Keys -> Publishable and secret API keys -> Secret keys
SUPABASE_SECRET_KEY=your_secret_key_here

# Your Google Gemini API Key (for the AI Quote Estimator chatbot and the CRM AI Co-Pilot)
# Get your FREE key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional. Gemini model used by the AI endpoints. Defaults to gemini-3.6-flash.
# Never configure the deprecated gemini-1.5-flash here.
GEMINI_MODEL=gemini-3.6-flash

# CRM login password. Choose a long, unique value; do not use a public/default password.
CRM_ADMIN_PASSWORD=replace_with_a_long_unique_crm_password

# Random secret used to sign CRM session cookies. Generate with: openssl rand -base64 48
CRM_SESSION_SECRET=replace_with_a_random_session_signing_secret

# =====================================================================
# NOTE: When deploying live on Netlify, you must also paste all variables above
# into the "Environment Variables" section of your Netlify Site Settings!
# =====================================================================
